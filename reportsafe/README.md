# Reportsafe

Scaffold for the Reportsafe app.

Structure:

- client/ — React app (Vite)
- server/ — Express + Mongoose API

Next steps:
1. cd client && npm install
2. cd server && npm install
3. Start dev servers (`npm run dev`) in each folder
